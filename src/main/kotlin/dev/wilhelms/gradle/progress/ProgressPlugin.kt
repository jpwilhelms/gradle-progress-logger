package dev.wilhelms.gradle.progress

import org.gradle.api.Plugin
import org.gradle.api.Project

class ProgressPlugin : Plugin<Project> {
    override fun apply(project: Project) {
        // Dummy plugin for testing classpath
    }
}
